Comments are written with a # symbol

### Command:
	**print(STR)**: Print the STR to the terminal
		Works with "STR", 'STR', or """STR""" for multiline strings
		Works with Concatenation (ex: print("This is " + "a string"))
		if STR == \n, a newline is printed
		
